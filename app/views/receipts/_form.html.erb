<%= simple_form_for(@receipt) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :owner_name %>
    <%= f.input :owner_adress %>
    <%= f.input :representative %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
